/*var min= 1;
		// IT MIN IS THE MINIMUM NUMBER IN THE RANGE
		var max=100;
		//This max is the maximum number in the range
		var range= max-min+1;
		// This is the range where you have to give the number within this range only
		var number=(Math.floor(Math.random()*range)+min);
        // HERE Math.floor IS UESD TO ROUND OFF THE DECIMAL NUMBER TO THE NEAREST WHOLE NUMBER
        console.log(number);*/

		//document.getElementById("rhint").innerHTML = "Opps sorry you loss the game!";
